{
  "name": "@repo/wa-sender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Fail-closed, idempotent WhatsApp Cloud API sender with safe billing normalization.",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
